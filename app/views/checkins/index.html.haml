%h1 Listing checkins

%table
  %thead
    %tr
      %th Mentor
      %th School
      %th Checkin time
      %th Latitude
      %th Longitude
      %th Checkout time
      %th Latitude
      %th Longitude
      %th
      %th
      %th

  %tbody
    - @checkins.each do |checkin|
      - checkout = checkin.correspond_checkout
      %tr
        %td= checkin.mentor.name
        %td= checkin.school.name
        %td= checkin.checkin_time.strftime("%m/%d/%Y %H:%M:%S")
        %td= checkin.lat
        %td= checkin.lon
        - unless checkout.nil?
          %td= checkout.checkout_time.strftime("%m/%d/%Y %H:%M:%S")
          %td= checkout.lat
          %td= checkout.lon
        %td= link_to 'Show', checkin
        %td= link_to 'Edit', edit_checkin_path(checkin)
        %td= link_to 'Destroy', checkin, method: :delete, data: { confirm: 'Are you sure?' }

%br

= link_to 'New Checkin', new_checkin_path
